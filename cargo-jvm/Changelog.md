# 0.1.2 - WIP

(still accumlating changes, not yet released)

# 0.1.1 - current stable


- Added support for `cdylib` crates in `cargo jvm package --lib`
- `cargo jvm doctor` emits more info about specific `cargo-jvm` and backend versions/git commits.
- Installs the backend's pinned nightly and components when needed, and forces
  Cargo, rustc, and standard-library preparation to use that toolchain.

# 0.1.0

- Initial release of `cargo-jvm`
